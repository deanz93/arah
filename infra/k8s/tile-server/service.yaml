apiVersion: v1
kind: Service
metadata:
  name: tile-server
  namespace: arah
spec:
  selector:
    app: tile-server
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
