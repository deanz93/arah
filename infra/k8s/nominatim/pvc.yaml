apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nominatim-data-pvc
  namespace: arah
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3
  resources:
    requests:
      storage: 100Gi
