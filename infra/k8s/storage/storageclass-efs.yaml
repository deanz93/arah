apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: efs-sc
provisioner: efs.csi.aws.com
parameters:
  provisioningMode: efs-ap
  fileSystemId: REPLACE_WITH_EFS_ID   # Set after Terraform creates EFS
  directoryPerms: "700"
reclaimPolicy: Retain
volumeBindingMode: Immediate
